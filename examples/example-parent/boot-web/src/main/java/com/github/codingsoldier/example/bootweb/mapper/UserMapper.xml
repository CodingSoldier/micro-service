<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.codingsoldier.example.bootweb.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.github.codingsoldier.example.bootweb.entity.UserEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
        <result column="phone" property="phone"/>
        <result column="date_of_birth" property="dateOfBirth"/>
        <result column="create_id" property="createId"/>
        <result column="update_id" property="updateId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id
        , name, age, phone, date_of_birth, create_id, update_id, create_time, update_time, deleted
    </sql>

    <select id="pageQuery" resultType="com.github.codingsoldier.example.bootweb.vo.UserDetailVo">
        select *
        from user
        <where>
            deleted=0
            <if test="pageDto != null">
                <if test="pageDto.name != null and pageDto.name != ''">
                    AND name LIKE CONCAT('%',#{pageDto.name},'%')
                </if>
                <if test="pageDto.age != null">
                    AND age=#{pageDto.age}
                </if>
            </if>
            ORDER BY id DESC
        </where>
    </select>

</mapper>
